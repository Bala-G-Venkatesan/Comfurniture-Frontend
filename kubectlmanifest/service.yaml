apiVersion: v1
kind: Service
metadata:
  name: loadbalancer-service
  labels:
    app: comfurniture
spec:
  type: loadbalancer
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: comfurniturepods
