apiVersion: v1
kind: Service
metadata:
  name: furnituredotcom
  labels:
    app: comfurniture
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: comfurniturepods
